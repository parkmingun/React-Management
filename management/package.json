{
    "name": "management",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn cilent\""
    },
    "dependencies": {
        "body-parser": "1.20.2",
        "express": "^4.16.4",
        "nodemon": "^2.0.22"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
